import os
import preprocessing as pp

flag = 0
dict={}
with open('../data/reason/reason/labels/abortion.txt') as f1:
    for line in f1:
        if line=='\n': 
            continue
        if '-' in line:
            key = line
        else:
            dict[key] = line
            
print dict

'''
for filename in os.listdir(os.getcwd()):
	with open(filename,'r') as f:
		#print("\n\n\n********************"+filename + "\n")
		if(flag > 0):
			print("\n\n\n********************"+filename + "\n")
		max1 = 0
		flag = 0
		for line in f:
			for word in line.split(':'):
				if "Thread" == word:
'''
